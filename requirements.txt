# ---------------------------------------------------------------------------
# Agent layer (install this first — no audio hardware or GPU needed)
# ---------------------------------------------------------------------------
langgraph>=1.0
langgraph-checkpoint-sqlite>=2.0
numpy>=2.0
python-dotenv>=1.0
httpx>=0.27
requests>=2.31        # Wikipedia: Wikimedia's edge blocks httpx by fingerprint, not requests
openai>=1.50          # Groq and OpenAI both speak this SDK (agents/llm.py)

# hybrid retrieval: BM25 keyword side + multilingual dense side (ONNX, no torch)
rank-bm25>=0.2.2
fastembed>=0.4

# PDF text extraction
pymupdf>=1.24

# web search fallback (no API key)
ddgs>=9.0

# tests
pytest>=8.0

# ---------------------------------------------------------------------------
# Realtime voice layer — install once the agent layer passes its tests.
# Large downloads (Silero VAD, Whisper weights). See requirements-audio.txt.
# ---------------------------------------------------------------------------
livekit-agents[silero,rime]~=1.5
faster-whisper>=1.1
websockets>=13.0
audioop-lts>=0.2; python_version >= "3.13"
